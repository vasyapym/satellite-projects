export function Footer() {
  return (
    <footer className="site-footer">
      <div className="container">
        Built as an extensible platform — shell + satellites. © {new Date().getFullYear()}
      </div>
    </footer>
  );
}
